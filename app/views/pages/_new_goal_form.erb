<%= simple_form_for([@goal], html: { class: "new-task-container" }) do |f| %>
  <%= f.input :title, input_html: { class: 'task-input', placeholder: "What's your new goal?" }, label: false, wrapper: false %>
<% end %>
